rebateControllers.controller('EditProfileController', ['$scope', '$http','$window','$location','broadcastService', function ($scope, $http,$window,$location, broadcastService) {
    
    /*$scope.loggedIn = sharedService.message || "NO";
    jQuery(document).ready(function($){
        $(".owl-carousel").owlCarousel();
    });
    
    $scope.$on('handleBroadcast', function() {
        $scope.loggedIn = sharedService.message;
        console.log('Company desc Controoler : ' + $scope.loggedIn);
    }); */
}]);